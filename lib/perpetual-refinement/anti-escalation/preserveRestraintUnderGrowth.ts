export function preserveRestraintUnderGrowth(text: string) {
  return text
    .replace(/\bscale attention\b/gi, "protect calmness")
    .replace(/\bgrow faster\b/gi, "grow carefully")
    .replace(/\bpush adoption\b/gi, "stay useful");
}
