export function deriveHumaneObservation() {
  return [
    "listen for recurring friction before adding new surfaces",
    "prefer simplification before expansion",
    "treat calmer use as a success condition",
  ];
}
