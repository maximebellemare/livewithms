export function deriveEnoughnessBoundaries() {
  return [
    "Do not add new layers when calmer refinement would solve the problem.",
    "Do not expand visible intelligence if a quieter system already works.",
    "Do not add novelty to prove progress when steadiness is already enough.",
  ];
}
