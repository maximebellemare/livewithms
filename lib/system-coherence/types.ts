export type CoherenceAdaptiveState =
  | "LOW_ENERGY"
  | "OVERWHELMED"
  | "WITHDRAWN"
  | "STABLE"
  | "REFLECTIVE";

export type CoherenceTone = "quiet" | "grounded" | "reflective";

export type CoherenceBurden = "low" | "moderate" | "high";
